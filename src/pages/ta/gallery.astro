---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { galleryData } from '../../data/gallery.js';
---

<BaseLayout lang="ta" title="புகைப்பட தொகுப்பு" description="வள்ளலார் ஆதியினத்தின் ஆன்மீக நிகழ்வுகள் மற்றும் திருவிழாக்களின் புகைப்படக் காட்சிகளைக் கண்டு மகிழுங்கள்.">
  <div class="max-w-6xl mx-auto px-6 py-12">
    <h1 class="text-4xl font-bold text-amber-900 mb-12 border-b-2 border-amber-200 pb-4">
      புகைப்பட தொகுப்பு
    </h1>

    {galleryData.map((event) => (
      <section class="mb-16">
        <!-- Event Name Header -->
        <h2 class="text-2xl font-semibold text-gray-800 mb-6 flex items-center">
          <span class="w-2 h-8 bg-[#FF9933] mr-3 rounded-full"></span>
          {event.eventNameTa}
        </h2>

        <!-- Image Grid for this specific event -->
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
          {event.images.map((img) => (
            <div class="group aspect-square overflow-hidden rounded-xl bg-gray-100 border border-amber-100 shadow-sm hover:shadow-xl transition-all duration-300">
              <a href={img.url} target="_blank" rel="noopener noreferrer" class="block w-full h-full">
                <img 
                  src={img.url} 
                  alt={img.altTa} 
                  class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                  loading="lazy"
                />
              </a>
            </div>
          ))}
        </div>
      </section>
    ))}
  </div>
</BaseLayout>